# GridOpsAgent 启动说明

● 重启项目 3 步

  1. 启动基础设施

  docker run -d --name mysql-gridops -p 3307:3306 -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=power_aiops mysql:8.0 --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
  docker run -d --name redis-gridops -p 6379:6379 redis:7-alpine

  ▎ 如果容器已存在但未启动：docker start mysql-gridops redis-gridops

  2. 确认环境变量

  echo $DEEPSEEK_API_KEY

  空的就设一下：
  export DEEPSEEK_API_KEY=你的key

  3. 启动服务

  cd E:\code\电网agent项目\GridOpsAgent-main

  # 先启 MCP Server（9901）
  nohup mvn -pl power-tools-mcp-server spring-boot:run > mcp-server.log 2>&1 &

  # 等 15 秒再启主应用（9900）
  sleep 15
  nohup mvn -pl grid-ops-agent-app spring-boot:run > server.log 2>&1 &

  验证：
  curl http://localhost:9900/actuator/health   # 返回 {"status":"UP"} 即可

  浏览器打开 http://localhost:9900/ 就能用了。

  
## 一、项目简介

电网智能运维 Multi-Agent 平台，基于 Spring Boot 3.2.0 + Spring AI + LLM 构建。

**两个模块：**

| 模块 | 端口 | 角色 |
|------|------|------|
| `power-tools-mcp-server` | 9901 | MCP Server，暴露电力系统查询工具 |
| `grid-ops-agent-app` | 9900 | 主应用，Graph 编排 + RAG + RBAC + 审计 |

## 二、环境要求

| 依赖 | 版本 | 用途 |
|------|------|------|
| JDK | 17+（推荐21） | 编译运行 |
| Maven | 3.9+ | 项目构建 |
| MySQL | 8.x | 业务数据，端口 3307 |
| Redis | 6.x+ | 缓存，端口 6379 |
| Docker | 27+ | 运行 MySQL / Redis / Milvus 容器 |
| LLM API Key | DeepSeek 或 DashScope | 大模型调用 |

> **说明：** Milvus 向量数据库可选（不启用时使用内存向量存储）。当前配置默认启用 Milvus。

## 三、基础设施准备

### 3.1 启动 MySQL（端口 3307，数据库 power_aiops）

```bash
docker run -d --name mysql-gridops \
  -p 3307:3306 \
  -e MYSQL_ROOT_PASSWORD=root \
  -e MYSQL_DATABASE=power_aiops \
  mysql:8.0 --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
```

### 3.2 启动 Redis（端口 6379）

```bash
docker run -d --name redis-gridops -p 6379:6379 redis:7-alpine
```

### 3.3 （可选）启动 Milvus 向量数据库

```bash
docker-compose -f vector-database.yml up -d
```

> Attu 管理界面: http://localhost:8001

## 四、配置检查

### 4.1 主应用配置

文件: `grid-ops-agent-app/src/main/resources/application.yml`

关键配置项：

```yaml
# DeepSeek API（当前默认）
spring.ai.openai.api-key: ${DEEPSEEK_API_KEY:sk-xxx}
spring.ai.openai.base-url: https://api.deepseek.com
spring.ai.openai.chat.options.model: deepseek-v4-flash

# MySQL 连接（端口 3307）
spring.datasource.url: jdbc:mysql://localhost:3307/power_aiops
spring.datasource.username: root
spring.datasource.password: root

# MCP 客户端（连接 MCP Server，使用 SSE 传输）
spring.ai.mcp.client.sse.connections.power-tools.url: http://localhost:9901
```

重要提示：
- **MCP 传输协议**：MCP Server 使用 SSE（Server-Sent Events）传输，主应用的客户端配置必须用 `sse` 而不能用 `streamable-http`，否则会报 404
- 如需更换 LLM，修改 `spring.ai.openai.*` 配置即可（兼容 OpenAI 接口规范）
- Milvus 如不可用，可将 `milvus.enabled` 设为 `false` 使用内存向量存储

### 4.2 MCP Server 配置

文件: `power-tools-mcp-server/src/main/resources/application.yml`

```yaml
server.port: 9901
spring.ai.mcp.server.type: SYNC
power.mock-enabled: true   # 开启 Mock 模式（无需真实 SCADA/PMS 系统）
```

## 五、启动步骤

### 5.1 编译项目

```bash
cd GridOpsAgent-main
mvn clean compile
```

### 5.2 启动 MCP Server（必须先启动）

```bash
# 前台运行
mvn -pl power-tools-mcp-server spring-boot:run

# 后台运行
nohup mvn -pl power-tools-mcp-server spring-boot:run > mcp-server.log 2>&1 &
```

确认启动成功（日志出现 "Registered tools: 5"）：

### 5.3 启动主应用

```bash
# 前台运行
mvn -pl grid-ops-agent-app spring-boot:run

# 后台运行
nohup mvn -pl grid-ops-agent-app spring-boot:run > server.log 2>&1 &
```

### 5.4 验证服务

```bash
# 健康检查
curl http://localhost:9900/actuator/health    # 返回 {"status":"UP"}

# MCP Server
curl http://localhost:9901/sse -H "Accept: text/event-stream"  # 返回 session ID

# Milvus 健康检查
curl http://localhost:9900/milvus/health
```

## 六、API 接口概览

| 模块 | 路径前缀 | 说明 |
|------|----------|------|
| 对话 | `/api/chat` | 同步对话、SSE 流式对话、会话管理 |
| 告警 | `/api/alarm` | 告警接收、诊断、状态查询 |
| 知识库 | `/api/knowledge` | 文档上传、搜索、版本管理 |
| 审批 | `/api/approval` | 高风险操作审批 |
| 工具 | `/api/tools` | 工具列表与搜索 |
| 观测 | `/api/observability` | 链路追踪、节点耗时 |
| 技能 | `/api/skills` | 技能列表与选择 |

测试对话：

```bash
curl -X POST http://localhost:9900/api/chat \
  -H "Content-Type: application/json; charset=UTF-8" \
  -d '{"question":"查询变压器TR-110KV-001的运行状态"}'
```

## 七、常见问题

### Q1: 主应用启动报 404 连接 MCP Server
**现象：** `McpTransportException: 404 Not Found from POST http://localhost:9901/mcp`
**原因：** MCP 客户端传输协议配置错误。MCP Server 使用 SSE 传输而非 Streamable HTTP。
**解决：** 确认 `application.yml` 中 MCP 客户端配置为 `sse` 而非 `streamable-http`。

### Q2: MySQL 连接被拒绝
**现象：** `CommunicationsException: Communications link failure`
**解决：** 确认 MySQL 容器在端口 3307 运行：`docker ps | grep mysql-gridops`

### Q3: LLM 调用失败
**现象：** "The template string is not valid" 或其他 API 错误
**解决：** 检查 `spring.ai.openai.api-key` 是否有效，确认 API Key 额度充足。

### Q4: Milvus 连接超时
**现象：** Milvus 健康检查失败
**解决：** 启动 Milvus 容器：`docker-compose -f vector-database.yml up -d`；或临时关闭：`milvus.enabled: false`

## 八、停止服务

```bash
# 停止 Spring Boot 服务
pkill -f "grid-ops-agent-app"
pkill -f "power-tools-mcp-server"

# 停止容器
docker rm -f mysql-gridops redis-gridops
docker-compose -f vector-database.yml down
```

## 九、已修复的问题

本次启动过程中修复了以下配置问题：

1. **MCP 传输协议不匹配**：将主应用 `application.yml` 中 `streamable-http` 改为 `sse`，与 MCP Server 的 `WebMvcSseServerTransportProvider` 保持一致。
